server:
  port: 9400
  servlet:
    context-path: /

app:
  errors:
    verbose: true

spring:
  application:
    name: profile-service
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://auth-service/.well-known/jwks.json
  datasource:
    url: jdbc:postgresql://localhost:5432/profile_db
    username: profile_user
    password: profile_pass
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: true
  sql:
    init:
      mode: never

management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    registry-fetch-interval-seconds: 5
    initial-instance-info-replication-interval-seconds: 5
  instance:
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 10

profile:
  avatar:
    endpoint: http://minio:9000
    access-key: ${MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${MINIO_SECRET_KEY:minioadmin}
    bucket: user-avatars
    prefix: user-avatars/
    secure: false
    upload-expiry-seconds: 900
    view-expiry-seconds: 900
  iam:
    base-url: http://iam-service
    roles-path: /iam/internal/v1/users/{accountId}/roles
    assign-role-path: /iam/api/v1/assign/user/{accountId}/role/{roleId}
    list-roles-path: /iam/api/v1/roles
    internal-auth-header: X-Internal-Token
    internal-auth-value: ${INTERNAL_TOKEN_FOR_IAM:dev-service-token}
    seller-role-name: SELLER
    request-timeout-ms: 3000
